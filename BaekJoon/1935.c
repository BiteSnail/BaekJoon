#include <stdio.h>
#include <string.h>

int main(void){
	int n;
	double Num[26];
	int Ntop=-1;
	char expression[200];
	int len;
	memset(Num, 0, 26);

	scanf("%d", &n);
	scanf("%s", expression);
	
	len=strlen(expression);
	
	for(int i=0;i<len;i++){
		
	}

	return 0;
}